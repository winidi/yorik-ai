# Plan: Finanzen (Bankkonten-App) — Umsätze lesend, mehrere Konten, geteilt oder privat

Stand 2026-09-25. Auslöser: Dirk hat die FinTS-Produktregistrierung bei
der DK abgeschickt und will jetzt echte Kontoumsätze (Sparkasse, ING)
in Yorik sehen können — read-only, kein Überweisen. Perspektivisch
Kategorisierung, wiederkehrende Zahlungen, Prognose (siehe „Später").

## Bestandsaufnahme — was schon da ist

- **`backend/connectors/banking_fints.py`** — fertiger, funktionierender
  Read-only-FinTS-Connector (Operationen `accounts`/`balance`/
  `transactions`) über die `fints`-Bibliothek (bereits in `venv`
  installiert, Version 5.0.0). Nutzt `FinTS3PinTanClient`.
- **`backend/credential_store.py`** — Fernet-verschlüsselte
  Zugangsdaten-Ablage, nennt „banking-fints“ explizit als Beispiel im
  eigenen Docstring.
- **Aber:** Der bestehende Connector ist **ein einziger globaler Slot**
  (`credential_store.get("banking-fints")`, ein fester Name, kein
  `owner_user_id`, kein Konzept „mehrere Konten“). Passt für „Admin hat
  ein Konto“, nicht für „Dirk hat Sparkasse+ING privat, dazu ein
  gemeinsames Konto mit Beate“. Der Connector-Code (FinTS-Aufrufe,
  Serialisierung) ist trotzdem die richtige Referenz/Bibliothek — nur
  die Speicherung muss wie bei `email_accounts` werden: eine Zeile pro
  Konto, `credential_key` zeigt in den `credential_store`.
- **Sichtbarkeitsmodell existiert schon** (`backend/spaces.py`,
  `row_filter`): eine Zeile gehört ihrem Owner, ist in dessen Space
  sichtbar, kann pro Zeile geteilt werden. Genau das braucht „meine
  Konten privat, gemeinsames Konto für beide“ — persönliches Konto im
  privaten Space des Owners, gemeinsames Konto im Shared-Space oder mit
  `row_shares`-Zeilen für beide.
- **Migrationen:** aktueller Stand `161_address_suggestions_per_person.sql`
  → nächste Nummer `162`.
- **Kein bestehender Finanz-Plan** in `docs/plans/` — das hier ist der
  erste.

## Ziel (dieser Durchlauf)

1. Mehrere Bankkonten pro Nutzer verbinden (Sparkasse, ING, beliebig
   viele), PIN-Eingabe nur direkt im Browser des Nutzers, nie über den
   Chat oder mich.
2. Sichtbarkeit wie beim Rest von Yorik: eigenes Konto sieht nur der
   Owner; ein Konto kann als „Shared“ angelegt werden, dann sehen es
   alle im Haushalt (z. B. Dirk + Beate gemeinsames Konto).
3. Umsätze werden synchronisiert und lokal gespeichert (nicht bei
   jeder Chat-Frage live gegen die Bank gefragt — TAN-Reibung,
   Geschwindigkeit).
4. Chat kann Fragen dazu beantworten („was hab ich für Lebensmittel
   ausgegeben“) über Skills, die auf den gespeicherten Umsätzen
   arbeiten.
5. Eine echte Seite (App) in Yorik: Konto verbinden, Kontostand,
   Umsatzliste, nach Kategorie filtern.

## Die fünf Schichten (nach `docs/ADDONS.md`)

| Schicht | Inhalt | Wo |
|---|---|---|
| 1. Daten | `bank_accounts` (owner_user_id, Bank-URL/BLZ/User, `credential_key`, IBAN, Space/Sichtbarkeit), `bank_transactions` (Konto-Referenz, Datum, Betrag, Gegenpartei, Verwendungszweck, Kategorie, Dedup-Hash) | `migrations_pg/162_bank_accounts.sql` |
| 2. Modul | Konto anlegen/testen, Sync (FinTS-Abruf → normalisieren → dedupen → speichern), einfache Kategorisierung (Regeln zuerst, LLM-Fallback für Unbekanntes) | `backend/bank_accounts.py`, `backend/bank_sync.py` |
| 3. Skills | `list_bank_accounts`, `show_transactions`, `spending_summary` (alle read-only, arbeiten auf der lokalen Tabelle, nicht live gegen FinTS) | `backend/skills/<name>/` |
| 4. UI | Konto-Verbinden-Formular (PIN-Feld direkt im Browser, nie über Chat), Kontoübersicht + Umsatzliste, Sichtbarkeits-Auswahl (privat/geteilt) beim Anlegen | `frontend-react/src/apps/finance/` |
| 5. Tests + Doku | pytest für Sichtbarkeit (eigenes/fremdes/geteiltes Konto), ein echter curl-Testlauf, Hilfe-Seite, Changelog | `tests/test_bank_accounts.py`, `docs/help/` |

## Regeln (Sicherheit zuerst)

- **PIN nie im Chat, nie bei mir.** Eingabe ausschließlich über ein
  echtes Formular im Browser des Nutzers, das direkt an die API
  sendet — gleiches Muster wie beim E-Mail-Konto-Setup
  (`POST /api/email/accounts`). Ich baue das Formular, tippe aber
  niemals selbst Zugangsdaten hinein.
- **Sichtbarkeit:** `owner_user_id` + `visibility` (`private` default,
  `shared` für Haushaltskonten) auf `bank_accounts`; `bank_transactions`
  erbt die Sichtbarkeit ihres Kontos. Kein Admin-Ausnahme-Zugriff auf
  private Konten fremder Personen (gleiche Regel wie bei Recordings).
- **Nur lesend.** Keine Überweisungs-Funktion, nirgends — auch nicht
  als spätere Ausbaustufe ohne explizite neue Entscheidung.
- **Sync, kein Live-Abruf pro Chat-Frage.** TAN-Reibung vermeiden;
  Skills lesen die lokale Tabelle, ein Hintergrund-Job synchronisiert
  periodisch (`backend/workers.py`-Muster).

## Später (nicht dieser Durchlauf, aber mitgedacht)

- Erkennung wiederkehrender Zahlungen, Prognose, Finanzcheck-Dashboard
  (siehe frühere Unterhaltung — bewusst zurückgestellt, bis die
  Datenbasis mit echten Umsätzen steht und sich kalibrieren lässt).
- PDF-Formular-Ausfüllen/Versand-Vorbereitung (bereits gebaut:
  `fill_pdf_form`, `prepare_email`, `PdfFormEditModal`) — separates
  Feature, nicht Teil dieser App.

## Protokoll (wird während des Baus fortgeschrieben)

- 2026-09-25: Plan geschrieben, Bestandsaufnahme abgeschlossen.
- 2026-09-25: Schicht 1 (Daten) — `migrations_pg/162_bank_accounts.sql`
  angewendet: `bank_accounts` (owner_user_id, space_id, credential_key
  pro Konto) + `bank_transactions` (dedup_hash, Kategorie-Spalte).
  `bank_accounts` in `backend/spaces.py` registriert
  (`TABLE_AREA["bank_accounts"]="finance"`, `owner_col`), damit
  `row_filter` greift — nutzt den schon vorhandenen, seit Setup
  mitangelegten "Finance"-Space (`slug='finance'`) für geteilte Konten.
- 2026-09-25: Schicht 2 (Modul) — `backend/bank_accounts.py` (CRUD +
  Verbindungstest, PIN geht direkt vom Browser in
  `credential_store`, nie über mich) und `backend/bank_sync.py`
  (FinTS-Abruf, Dedup, einfache Regel-Kategorisierung, 6h-Scheduler).
  Router + Scheduler in `backend/main.py` verdrahtet.
  **Echter Bug gefunden und gefixt:** `python-fints` 5.0.0 wirft seit
  v4 einen `TypeError`, wenn `product_id` leer ist — kein
  Bibliotheks-Platzhalter mehr wie früher angenommen. Fallback-Test-ID
  `"999999999999"` in `bank_accounts.py`/`bank_sync.py` ergänzt,
  klar als unverifizierter Community-Notbehelf kommentiert, bis die
  echte DK-Nummer da ist. Ohne den Fix crashte jeder Verbindungsversuch
  mit einer rohen Python-Fehlermeldung statt eines verständlichen
  Fehlers.
- 2026-09-25: Schicht 3 (Skills) — `list_bank_accounts`,
  `show_transactions`, `spending_summary`, alle read-only auf der
  lokalen Tabelle, `app:finance`-Tag fürs Opt-in-Gating. Laden sauber
  in der Registry.
- 2026-09-25: Schicht 4 (UI) — App `finance` in `backend/apps.py`
  registriert (opt_in=True, Icon 💶/Landmark), Route `/finance` +
  Dock-Eintrag (teal) im Frontend. `FinanceApp.tsx`: Kontenliste,
  Konto-verbinden-Formular (PIN-Feld ausschließlich hier), Umsatzliste,
  einfache Kategorie-Summe. Live im Browser getestet: App aktiviert,
  Seite lädt, Formular öffnet mit allen Feldern, Verbindungstest mit
  absichtlich falschen Zugangsdaten liefert einen sauberen 400-Fehler
  statt eines Absturzes, nichts wird bei einem Fehlversuch gespeichert
  (per Datenbank-Check bestätigt).
- 2026-09-25: Schicht 5 (Tests) —
  `tests/test_bank_accounts_visibility.py`: drei Tests (Owner sieht
  eigenes+geteiltes, nicht fremdes privates; anderes Haushaltsmitglied
  ebenso; kein Admin-Sonderzugriff auf fremdes privates Konto) — alle
  grün gegen die echte Test-Postgres. Volle Suite (`pytest tests`)
  parallel angestoßen, Ergebnis folgt.
- **Offen für einen echten End-to-End-Test:** Ein echtes Konto
  verbinden kann nur Dirk selbst, weil die PIN nie durch mich laufen
  darf — das Formular ist bereit, der erste echte Test mit
  Sparkasse/ING-Zugangsdaten steht noch aus.
- 2026-09-25: Volle Testsuite (`pytest tests`) einmal komplett
  durchgelaufen: 530 bestanden, 1 übersprungen, 0 fehlgeschlagen
  (5:15 Min). Erster Lauf mit `timeout 300` war fälschlich als
  "failed" gemeldet — das eigene Zeitlimit hat pytest mitten im Lauf
  abgewürgt und durch das `tail`-Pipe die komplette Ausgabe
  verschluckt, keine echten Testfehler.
- 2026-09-25: UX-Nachbesserung — Bank-Suche statt manueller FinTS-URL.
  `backend/fints_institutes.json` (2721 Einträge: Name, Ort, BLZ,
  FinTS-URL) einmalig aus `hbci4j/hbci4java`s `blz.properties`
  generiert (aktiv gepflegt; python-fints selbst hat keine
  Institutsliste). Ein erster Kandidat (pecunia-client-CSV) erwies
  sich als veralteter ~2005er-Schnappschuss ohne ING-Eintrag und wurde
  verworfen. `GET /api/bank/institutes?q=...` + `BankSearchField` im
  Formular (Debounce-Suche, füllt BLZ+URL automatisch, beide Felder
  bleiben sichtbar/editierbar). Live geprüft: „Kreissparkasse Peine"
  und „ING-Diba" gefunden, Felder korrekt befüllt.
